<%= form_for(@learning_resource) do |f| %>
  <% if @learning_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@learning_resource.errors.count, "error") %> prohibited this learning_resource from being saved:</h2>

      <ul>
      <% @learning_resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :resourceurl %><br>
    <%= f.text_field :resourceurl %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :section_id %><br>
    <%= f.number_field :section_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
